<%# TODO: Needs styling! %>

<%= link_to private_chatroom_path(chat) do  %>
  <p>Chat with <%= chat.creator_id == current_user.id ? User.find(chat.joiner_id).first_name : User.find(chat.creator_id).first_name %></p>
<% end %>
